import styled from 'styled-components'

export const SearchBox = styled.input`
    flex: 1;
    height: 7rem;
    padding: 0 2.4rem;
    border: 0;
    font-size: var(--font-regular);
    color: var(--secondary);
    border: 0;
`;